[package]
name = "nix-doc-plugin"
version = "0.5.5"
edition = "2018"

[lib]
crate_type = ["cdylib"]
test = false

[build-dependencies]
cc = "1.0.58"
pkg-config = "0.3.18"

[dependencies]
nix-doc = { path = "../nix-doc" }
